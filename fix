// Directional safety: NEVER create opposite-direction orders
if(m_plan.signal == "BUY")
{
   m_plan.sell_orders = 0;
}
else if(m_plan.signal == "SELL")
{
   m_plan.buy_orders = 0;
}
else
{
   // HOLD or unknown signal = absolutely no orders
   m_plan.buy_orders = 0;
   m_plan.sell_orders = 0;
}
